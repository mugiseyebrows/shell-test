name: main
on: 
- push
jobs:
  main:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm i
      - run: node index.js shell.hec.to 8857
        shell: cmd

      